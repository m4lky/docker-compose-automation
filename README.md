# docker-compose
Repo used for active docker-compose files

```bash
.
├── infra
│   ├── monitoring
│   │   ├── docker-compose.yml
│   │   └── prometheus
│   │       └── prometheus.yml
│   └── reverse-proxy
│       └── docker-compose.yml
└── services
    ├── jenkins
    │   └── docker-compose.yml
    ├── portainer
    │   └── docker-compose.yml
    ├── transmission
    │   └── docker-compose.yml
    └── unifi
        └── docker-compose.yml
```
